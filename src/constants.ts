import {environment} from "./environments/environment";

export const BASE_URL = environment.baseUrl;
export const ENDPOINT_CLIENTS = `${BASE_URL}/clients`
export const APP_TITLE = 'TBC Clients Management App'
